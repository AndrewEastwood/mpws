jsapiCategoryList = '{
    "data" : {
        "action" : "select",
        "condition": {
            "filter": "Enabled (=) ?",
            "values": [1]
        },
        "source" : "shop_categories",
        "fields": ["ID", "ExternalKey", "Name", "Description", "Enabled", "DateCreated"],
        "output": "DEFAULT"
    }
}';

jsapiOriginList = '{
    "data" : {
        "action" : "select",
        "condition": {
            "filter": "Enabled (=) ?",
            "values": [1]
        },
        "source" : "shop_origins",
        "fields": ["ID", "ExternalKey", "Name", "Description", "Enabled", "DateCreated"],
        "output": "DEFAULT"
    }
}';